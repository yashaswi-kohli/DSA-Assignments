
1. Write a program to print whether a number is even or odd, also take input.


#include <iostream>
using namespace std;
int main()
{
    int num;
    cout<<"Enter number: ";
    cin>>num;
    if(num%2==0)
    {
        cout<<num<<" is even.";
    }
    else
    {
        cout<<num<<" is odd.";
    }
    return 0;
}                             

-----------------------------------------------------------------------------------

2. Take name as input and print a greeting message for that name.


#include <iostream>
using namespace std;
int main()
{
    string a;

    cout<<"Enter the name: ";
    cin>>a;

    cout<<"Hello "<<a<<"!";
    return 0;
}    


-----------------------------------------------------------------------------------

3. Write a program to input principle, time, and rate (P, T, R) from the user and
find Simple Interest.


#include <iostream>
using namespace std;
int main()
{
    int Simple_Interest,Principal,Interest_Rate,Time_Period;

    cout<<"Enter the Principal: ";
    cin>>Principal;

    cout<<"Enter the Interest Rate: ";
    cin>>Interest_Rate;

    cout<<"Enter the Time Period: ";
    cin>>Time_Period;

    Simple_Interest = (Principal * Interest_Rate * Time_Period);

    cout<<"The Simple Interest Rate is: "<<Simple_Interest;
    return 0;
}                


-----------------------------------------------------------------------------------

4. Take in two numbers and an operator (+, -, *, /) and calculate the value.
(Use if conditions)



-----------------------------------------------------------------------------------

5. Take 2 numbers as input and print the largest number.

#include <iostream>
using namespace std;
int main()
{
    int a, b;
    cout<<"Enter two numbers: "<<endl;
    cin>>a>>b;
    if(a<b)
    {
        cout<<b<<" is the largest number.";
    }
    else
    {
        cout<<a<<" is the largest number.";
    }
    return 0;
}       

-----------------------------------------------------------------------------------

6. Input currency in rupees and output in USD.


#include <iostream>
using namespace std;
int main()
{
    int a,b;
    cout<<"Enter the amount in rupees: ";
    cin>>a;
    b = a/73;
    cout<<a<<" rupees is "<<b<<" dollars.";
    return 0;
}    

